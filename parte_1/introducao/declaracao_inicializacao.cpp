int main()
{
    int a, A, maior, Maior, mAior, maIor; //São todas diferentes.
    int variavelInteira;
    float variavelReal;
    float velocidadeEmMPorS = 2.3f;
    bool desligado = true; //C++11
    const float PI = 3.14; //Variável constante. Não pode ser alterado. //C++11
    int idadeEmAnos(14); //C++14
    int numeroAtomico{ 20 }; //C++14 - Inicialização uniforme: não permite a atribuição de um tipo diferente
    double numeroReal{}; //0.0. Zero inicializador: atribui um valor padrão ao tipo e zero quando numérico
}
