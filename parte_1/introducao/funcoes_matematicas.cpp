import <iostream>;
import <cmath>;
using namespace std;

int main()
{
    auto base{2.5};
    auto expoente{1.5};
    auto potenciacao = pow(base,expoente);

    auto x{2.5};
    auto raizQuadradaDeX = sqrt(x);

    auto senoDeX = sin(x);

    auto logBase10DeX = log10(x);
    auto logaritmoNaturalDeX{log(x)};

    auto moduloDeX = fabs(x);

    int y = -4;
    auto moduloDeY{abs(y)};
}
